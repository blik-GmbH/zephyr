sample:
  name: Low Power state Sample for frdm-kw41z
tests:
  pm.low_power_state:
    platform_whitelist: frdm_kw41z
    tags: power
    harness: console
    harness_config:
      type: multi_line
      regex:
        - "---->Low power state entry - CONSTANT LATENCY MODE----- Low power state exit !"
        - "---->Low power state entry - LOW POWER MODE ----- Low power state exit !"
